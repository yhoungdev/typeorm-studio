{
  "name": "typeorm-studio",
  "private": true,
  "scripts": {
    "dev": "bun run index.ts",
    "format": "prettier --write .",
    "dev:client": "npm run --workspace packages/client dev",
    "dev:server": "npm run --workspace packages/server dev",
    "lint": "npm run --workspace packages/client lint && npm run --workspace packages/server lint",
    "lint:fix": "npm run --workspace packages/client lint:fix && npm run --workspace packages/server lint:fix",
    "typecheck": "npm run --workspace packages/client typecheck && npm run --workspace packages/server typecheck"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^25.3.1"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@xyflow/react": "^12.10.1",
    "prettier": "^3.8.1"
  }
}
